/**
 * Used as a key to access components.
 * Opened panels are saved in localStore as PanelIds.
 */
export enum PanelComponentsIds {
  NETWORK = 'NETWORK',
  SERVER_STATUS = 'SERVER_STATUS',
  FACTIONS = 'FACTIONS',
  AGENT_ID = 'AGENT_ID',
  CONTRACTS = 'CONTRACTS',
  LEADERBOARDS = 'LEADERBOARDS',
  AGENT_DETAILS = 'AGENT_DETAILS',
  MARKETPLACE = 'MARKETPLACE',
  AGENT_TRANSACTIONS = 'AGENT_TRANSACTIONS',
  SHIPS = 'SHIPS',
}