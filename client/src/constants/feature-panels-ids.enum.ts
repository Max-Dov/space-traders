/**
 * Feature-panels.
 * App is expected to have only one instance of each panel opened.
 * Should be accessible from the app top bar.
 */
export enum FeaturePanelsIds {
  NETWORK = 'NETWORK',
  SERVER_STATUS = 'SERVER_STATUS',
  FACTIONS = 'FACTIONS',
  AGENT_ID = 'AGENT_ID',
}