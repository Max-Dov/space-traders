/**
 * <AppLayout /> provides feature panel props used by "Draggable" component required for panels drag-n-drop.
 * Note: not all panels are draggable.
 */
export interface CommonFeaturePanelProps {
  panelId: string;
  panelIndex: number;
}